services:
  overfit-checker:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: overfit-checker-web
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
